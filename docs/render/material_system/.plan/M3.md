# M3 — Pipeline and static-resource resolution

Parent: [Material System plans](../PLANS.md) · Roadmap: [TODO](../TODO.md)

Render-private resolution maps material intent to pipeline-cache keys and
static texture/sampler handles through `RenderResourceResolver`. Readiness is
Pending, Ready, or Failed with diagnostic text. Material System must not
receive `RenderBackend` or backend-native objects.

Acceptance: dependency deduplication and failed-resource suppression prevent
unready instances from being submitted.

Status: complete.
