# L2D4.4 — Packed Masks as Generic Passes

- Status: landed (2026-09-11) — [journal](../../.spec/journal/2026-09-11-live2d-l2d4-4.md)
- Parent: [L2D4 — Live2D Planning and Generic Render Submission](L2D4.md)
- Depends on: [L2D4.3](L2D4.3.md)
- Review: [runtime-result failure analysis](../.review/L2D4.4.md)

## Goal

Add deterministic ordinary and inverted Cubism clipping through one
module-owned packed RGBA atlas. Live2D calculates the mask plan and compiles it
into two ordinary generic Render passes; the executor remains semantic-free.

## CPU mask planner

Implement an SDK-free `Live2DMaskAtlasPlanner` that:

1. interns identical ordered mask-source sets as one context;
2. finds contexts used by at least one visible clipped drawable;
3. computes the union of their clipped drawable bounds from current positions;
4. expands non-empty bounds by the frozen 5% margin;
5. assigns active contexts deterministically by context index to atlas,
   channel, and region according to the L2D4.0 capacity table;
6. calculates model-to-mask and model-to-atlas-sample matrices;
7. rejects zero-area/non-finite bounds and capacity overflow with context data.

The planner returns plain values and has no Graphics or Cubism dependency.

## GPU resources and passes

When static data contains clipping, the proxy owns one sampled
`RGBA8_UNORM` target at the frozen resolution and the mask pipeline variants.
Creation fails if the backend cannot render to and sample that format.

Live2D planning produces this generic submission order:

```text
validate snapshot and describe one position upload
  -> plan active masks
  -> SubmissionPass(mask target cleared to 1, source draw commands)
  -> SubmissionPass(color target cleared transparent, ordered model draws)
  -> generic executor records both passes
```

The Live2D planner selects mask-source ranges, current geometry, texture,
culling, mask matrix, atlas channel, viewport, and region scissor according to
L2D4.0. It emits those as ordinary generic draw fields. Main masked draws bind
both the model texture and atlas texture and carry channel/polarity constants.
The shaders calculate texture alpha, atlas sampling, and ordinary/inverted
coverage per pixel. Unclipped drawables use the already-proven L2D4.3 path.

The generic executor preserves the authored pass order and never begins one
target while another is active. A failed Live2D mask plan publishes no generic
submission. A generic preparation failure records neither pass. Do not render
unclipped parts as an approximation.

## Tests

Pure planner tests cover:

- one context; identical-set sharing; multiple channels and subdivisions;
- deterministic assignment independent of consumer drawable order;
- bounds/margin/matrix numeric goldens;
- invisible consumers, empty source geometry, zero-area bounds;
- exact capacity and capacity-plus-one;
- ordinary and inverted polarity.

Backend readback cases cover:

- one source masking one drawable;
- two drawables sharing a context;
- contexts in different channels/regions;
- soft transparent texture edges without atlas bleed;
- inverted clipping;
- a moving mask source over repeated frame slots.

Compare center pixels exactly within the L2D4.0 channel tolerance and compare
filtered boundaries using its edge tolerance. Inspect at least one atlas
readback during development so an all-white/all-zero false positive cannot
pass only from final color.

## Gate

All planner tests and OpenGL/Vulkan clipped readbacks pass. Render tests prove
the same executor handles one-pass unmasked and two-pass masked submissions
without a mask-specific branch. The result contains no direct framebuffer/image
barriers above Graphics. Unsupported high-precision and offscreen cases fail
before proxy publication rather than selecting an approximate mask path.
