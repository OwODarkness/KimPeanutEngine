# L2D6.4 Playback Hardening and L2D7 Handoff — 2026-09-12

- Status: complete; L2D7 handoff accepted (2026-09-16).
- Scope: harden the instance-local playback adapter without changing Render,
  Graphics, Editor, Asset core, or the standalone viewer host boundary.

## What landed

- Split `AdvancePlayback` into named internal phases:
  `LoadParameterCheckpoint`, `ApplyPendingParameterWrites`,
  `UpdatePrimaryMotion`, `SavePrimaryCheckpoint`,
  `UpdateExpressionContribution`, `ApplyLateUpdateContributors`, and
  `UpdateModel`.
- Kept the persistent checkpoint after primary motion and before expression and
  late contributors. The late-update method is the L2D7 insertion point.
- Rejected invalid delta time before moving pending events or touching Cubism;
  negative delta coverage verifies parameter values remain unchanged.
- Added explicit bounded event-storage reservation and callback-overflow checks.
- Made immediate `StopMotion` token-local by finishing only the selected SDK
  queue entry. It no longer replaces the whole motion manager, so overlapping
  replacement fades remain cancellable independently.
- Kept `StopAllMotions` on the existing manager and added stable Product V1
  reimport-required diagnostics for typed motion/expression lookup misses.
- Added regression coverage for invalid delta atomicity and two simultaneous
  immediate cancellations.
- Added runtime Product V1 reimport-required and Product V2 no-animation
  instance coverage, plus a deferred screenshot command resolver so the
  standalone viewer can capture after its service is created on the render
  thread without racing command registration.

## Validation

- `cmake --build build --config Debug --target Live2DCoreTest --parallel 1` — passed.
- `ctest --test-dir build -C Debug -R 'ScreenshotCommandProviderTest|Live2DAssetTest|Live2DCoreTest' --output-on-failure` — 20/20 passed.
- `ctest --test-dir build -C Debug -R Live2D --output-on-failure` — 56/56 passed after the host capture registration.
- `cmake --build build --config Debug --target KimPeanutEngine --parallel 1` — passed.
- `cmake --build build-nolive2d --config Debug --target KimPeanutEngine --parallel 1` — passed.
- An attempted synthetic Cubism `UserData` injection into the checked-in Mao
  motion was rejected by the SDK parser and was not retained as a fixture;
  authored motion-user-event coverage remains a named follow-up from L2D6.3.
- OpenGL full-duration viewer: `--mode live2d-viewer --graphics-api opengl --agent-port 37373`; after six seconds `live2d.model_report` reported `update_sequence=2418`, then `capture.screenshot` exported `save/screenshots/validation/l2d6-4-opengl-full-duration.png`. The capture shows the complete model with eyes visible.
- Vulkan full-duration viewer: same run with `--graphics-api vulkan`; `update_sequence=2662`, and `save/screenshots/validation/l2d6-4-vulkan-full-duration.png` exported successfully with the complete model visible.
- Both viewer runs shut down after the controlled capture without a remaining `KimPeanutEngine` process; `git diff --check` passed.

## Closeout

The hardening matrix is accepted for the current locally licensed Hiyori/Mao
fixtures. The standalone viewer now exposes the existing Runtime screenshot
provider through a deferred host resolver, so capture can occur after the
authored motion has crossed its completion boundary while preserving the same
renderer-owned output path. No Render or Graphics source was changed by this
stage; the only new runtime dependency is the existing screenshot command
adapter in the viewer host.

## Fixture and ownership notes

The runs use the checked-in Hiyori and Mao fixtures and do not add proprietary
assets. Runtime playback remains instance-owned; immutable product data remains
asset-owned; GPU submission and capture readback remain renderer-owned.
Authored user-event strings remain deferred until a checked-in or importer-owned
fixture can be supplied without bypassing Cubism's motion parser.
