# AP1.0 — Startup profiler first slice

Date: 2026-09-09

## Landed

- Added Asset observation attribution for cumulative dependency wait,
  per-AssetType totals, source/decoded bytes, and a completion-indexed startup
  ledger of completed operations. The profiler can sort that ledger by index,
  total time, source time, or memory; durations at or above one second are
  displayed in seconds.
- Added the loading-only `Startup Profiler` ImGui window. It reads copied
  `StartupSnapshot` values, samples peak process memory through the existing
  platform seam, and does not acquire Asset, Runtime, or Render locks.
- Kept the runtime validation fixture small: `level/performance_profile.level`
  contains the Stanford bunny and Cerberus. Sponza remains the fixed large-scene
  stress case for later AP1 stages.

## Validation

- Debug build: `EditorUILib`, `AssetUnitTest`, `EditorUILifecycleTest`, and
  `KimPeanutEngine` built successfully after the Visual Studio SDK probe was
  run with elevated access.
- Focused CTest: 19/19 passed, including the completion-ledger and UI coverage.
- Full Debug CTest: 439/439 passed.
- Runtime smoke: OpenGL launch with
  `--startup-level level/performance_profile.level --agent-port 37373` reached
  the ready workspace; the log reported 24 registered assets, 23 load
  operations, 3 cache hits, and 16.934 s of Asset loading with the bunny and
  Cerberus fixture.

## Follow-up shutdown presentation

- The existing loading panel now switches to `Closing engine` when the render
  thread begins RuntimeContext teardown. Runtime cleanup reports seven visible
  stages covering gameplay, level, reflection/scripting, renderer, prepared
  assets, input, and window release; each stage publishes copied progress before
  the corresponding release work begins.

## Remaining AP1.0 work

The window is live startup attribution, not yet a persistent baseline report.
Controlled Debug/RelWithDebInfo and cold/warm filesystem-cache samples, plus a
startup capture held long enough to inspect the transient loading frame, remain
open. No production startup delay was added solely for visual capture.

## AP1.1 — Product verification

### Landed

- Added `Sha256WithZeroedRange`, which computes the content hash and the
  embedded-digest hash from the loaded byte vector without allocating a second
  product-sized integrity input.
- Native Texture and Model loaders compute both hashes once, pass the content
  hash into archive filename verification, and pass the pair into structural
  deserialization. Direct deserializer callers retain a safe fallback that
  computes the same pair without the old full-product clone.
- Replaced native cook-time `BuildIntegrityInput` copies with the same
  zeroed-range hashing contract. Malformed products, truncated digests,
  filename mismatches, and embedded-digest mismatches remain rejected.

### Reference gate and boundary

- Distill's `loader/src/packfile_io.rs` was inspected for immutable
  buffer/mapped-byte lifetime during packfile decoding.
- O3DE's `Code/Tools/AssetProcessor/native/utilities/assetUtils.cpp` and
  Godot's `doc/classes/HashingContext.xml` were inspected for chunked hashing
  patterns.
- This stage keeps KimPeanutEngine's existing owned product vector and
  database-free AssetProduct boundary. It adopts no mmap or package layer;
  those belong to later AP1.4 work.

### Validation

- Elevated Debug build succeeded for `AssetProduct`, `AssetNative`,
  `AssetRuntime`, and `AssetUnitTest` after the non-elevated build was blocked
  by the Visual Studio SDK probe directory permission.
- Focused Asset tests: 15/15 passed for ModelArchive hash verification,
  native Model format/loader behavior, and Texture import/round-trip paths.

## AP1.2 — GPU-native Texture compression

### Landed

- Extended the common texture contract with BC4 UNORM, BC5 UNORM, BC3 UNORM,
  and BC3 sRGB. Native Texture V2 validates raw and block-compressed mip sizes
  from the directory metadata while retaining a V1-compatible reader path.
- Added deterministic offline BC cooking selected by semantic: BC3 sRGB for
  color/generic, BC3 UNORM for packed linear, BC5 UNORM for normals, and BC4
  UNORM for opacity masks. Portable RGBA8/RGBA16F cooking remains available.
- Added Vulkan mappings and OpenGL RGTC/S3TC mappings with compressed mip
  upload calls. Backend capabilities report sampled support for each BC format;
  Render refuses an unsupported compressed payload rather than expanding it or
  uploading it through an uncompressed path.
- Added `cook-texture --compression portable|bc`; model import settings accept
  the same compression policy for later profile-driven recooking.

### Validation

- Elevated Debug build passed for `Graphics`, `RuntimeLib`, `AssetImport`,
  `TextureImportTest`, and `GraphicsContractTest`.
- Focused Asset/import tests passed 9/9, including native compressed round-trip,
  semantic format selection, deterministic bytes/hashes, and model-import
  compatibility. Graphics contract tests passed 15/15.

### Remaining boundary

The Sponza <=550 MB closure measurement, package-level logical-texture variant
map, BC7/ASTC profiles, and the final cross-backend Sponza capture remain open.
Material startup profile selection is now implemented before Asset dependency
resolution, so a backend does not cause both portable and BC products to load.

### AP1.2 correctness follow-up — BC5 normal reconstruction

- Documented the native `.texture` BC4/BC5/BC3 block layouts, byte-count rule,
  semantic mapping, normal-map convention, and explicit portable fallback in
  `engine/runtime/asset/README.md`.
- Updated `asset/shader/pbr_gbuffer.frag` to sample normal X/Y from RG and
  reconstruct the positive-Z tangent normal. This is required for BC5, whose
  third channel is not stored, and also makes portable RGBA8 normal sampling
  use the same convention.
- Validation: `glslc` compiled the modified PBR fragment shader successfully;
  the focused texture/graphics contract suite passed 5/5. The affected C++
  build was retried but stopped in the Visual Studio SDK probe with an access
  denied error before source compilation.

### AP1.2 profile tooling and size measurement

- Added `--compression portable|bc` to `KimPeanutAssetTool import/reimport`.
  The command now passes the selected policy into `ModelImportSettings`; the
  existing settings hash already includes the policy, so changing profiles
  invalidates the recorded import without changing Asset identity semantics.
- Measured the same 2048² quartz albedo through `cook-texture`: the portable
  RGBA8 sRGB product was 22,370,096 bytes and the BC3 sRGB product was
  5,592,908 bytes, a 75.0% payload reduction including native-container
  overhead. This is a product-size measurement, not yet a full startup
  residency or GPU visual measurement.
- The remaining profile-selection boundary was the logical-texture map:
  Material V2 now publishes an explicit portable/BC alternative map, and the
  startup Runtime selects one profile before Asset dependency loading.
- End-to-end temporary-archive validation imported
  `model/cerberus/cerberus_lp.fbx` with both profiles. Each archive published
  one Model, one Material, and one Texture product; the Texture was
  22,370,096 bytes portable and 5,592,908 bytes BC. No checked-in archive was
  modified.

### AP1.2 runtime profile selection and visual validation

- Material V2 now publishes a portable path plus an optional BC path for each
  cooked texture parameter. Runtime converts the initialized backend's sampled-
  format capabilities into an API-neutral Asset profile before startup. Asset
  then resolves exactly one dependency; Render validates the selected format.
- Reimported Cerberus into the checked-in archive with the BC profile. The
  material published both products: portable `22,370,096` bytes and BC
  `5,592,908` bytes. The generated material retains the portable `path` for
  backward compatibility and adds a `variants` object.
- Rebuilt the Debug Runtime and launched
  `level/performance_profile.level` through the local Runtime bridge on both
  Vulkan and OpenGL. Both reached the ready workspace and produced inspected
  captures: `save/screenshots/validation/ap12-profile-vulkan.png` and
  `save/screenshots/validation/ap12-profile-opengl.png`. The bunny scene was
  visible on both APIs with no obvious texture or normal-map artifact.
- Focused CTest and the affected Debug targets passed after the profile
  selection changes. The remaining AP1.2 boundary is package-level selection
  for non-material streaming resources, plus BC7/ASTC and the final Sponza
  closure/startup budget measurement.

### AP1.2 single-profile startup correction

- The first variant implementation loaded both portable and BC dependencies,
  which increased the Sponza startup result to 206 Assets, 312 operations, and
  194,436.996 ms. The correction adds an Asset-neutral
  `TextureVariantProfile`, selected after Render initializes its backend. The
  MaterialLoader now declares only the selected product; the alternate path is
  metadata and is not read during startup.
- Direct Vulkan Sponza validation after the correction reported 126 Assets,
  195 operations, and 81,457.986 ms. Its Render profile reported
  `textures=80`, `source_bytes=447,432,640`, and
  `resident_bytes=447,394,604`, proving the BC profile was selected without
  loading portable duplicates. The visual capture was exported to
  `save/screenshots/validation/ap12-single-profile-vulkan.png`.
- OpenGL Sponza capture was intentionally not waited on because the user will
  perform that large-scene validation manually. The focused profile-selection
  suite passed 21/21; the existing full Debug suite remains the final broad
  regression check.

### OpenGL scene viewport orientation correction

- OpenGL startup and rendering were healthy, but the editor viewport sampled
  the scene render target with ImGui's top-left UV convention while the OpenGL
  render-target image uses a bottom-left origin. `EditorImguiOpenglRenderer`
  now reverses only the V range for scene/debug images; Vulkan, scene camera
  math, and material texture UVs remain unchanged.
- Rebuilt `KimPeanutEngine` successfully. A default-OpenGL
  `level/performance_profile.level` run reached the ready workspace and
  completed its OpenGL render profile. The affected Render/Graphics regression
  suite passed 14/14.
